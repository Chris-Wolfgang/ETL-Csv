{
  "_comment": "Mutation-testing quality gate (#68). Measured ~69% at Standard level locally (net10.0). 'break' fails the run below this score; set conservatively for a non-flaky first CI run (CI timeout-mutant behavior can differ from local) and intended to be ratcheted up toward the observed CI score once a baseline exists. Run: `dotnet tool restore && dotnet stryker`.",
  "stryker-config": {
    "project": "Wolfgang.Etl.Csv.csproj",
    "test-projects": ["tests/Wolfgang.Etl.Csv.Tests.Unit/Wolfgang.Etl.Csv.Tests.Unit.csproj"],
    "target-framework": "net10.0",
    "concurrency": 4,
    "thresholds": { "high": 90, "low": 70, "break": 60 },
    "reporters": ["html", "json", "cleartext"],
    "mutation-level": "Standard"
  }
}
